---
fconf_templates:
  blue(one):
    - before ${one} after
    - this: that
  red():
    - r1
    - r2
  host(num): pfx${num}.bivio.biz
  inside():
    - un
    - deux
    - trois
  more_items(a1):
    item1: ein
    item2: zwei
    item3: ${a1}

top:
  pi: 3.14
  hosts: [42, 43, 44]

one:
  two: three('hello')
  blue: blue('here')
  empty:
  more_items(inside()):
  list:
    - r0
#TODO: need to get this to merge
    - red()
  mixed: mymap(host, ['5'])
  second_host: ${top.hosts.1}

next:
  apple: 1
  pear: 2
  best_food: ${top.pi}
