---
# Force
language: python
python:
  - "2.7"
addons:
  apt:
    packages:
      - screen
#install:
#  - "pip install -r requirements.txt"
#  - "python setup.py install"
script:
  - "python setup.py tox"
